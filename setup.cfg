[bdist_wheel]
universal = 0

[tool:pytest]
addopts = -ra
markers =
    hardware: requires access to physical hardware
    slow: longer-running tests
norecursedirs = debug build .venv
testpaths = tests
